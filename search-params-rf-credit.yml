DATASET: credit
SCORING: roc_auc
ESTIMATOR: rf
FOLDS: 10
HP_DIST:
  max_depth:
    type: integer
    min: 3
    max: 11
  max_features:
    type: float
    min: 0.1
    max: 1.0
  bootstrap:
    type: choice
    values: [True, False]
